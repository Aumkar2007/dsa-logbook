```mermaid
graph TD;
    A[When to use Two Pointers]-->B[Array/Linklist];
    B-->C[Either already sorted or sort krne se aasan hojayega];
    C-->D[Rearrange/Remove Duplicates/Merge inplace/ Subarrays];
    C-->E[Finding Pairs/Triplets/Quadruplets];
    C-->G[Zero Space, SpaceComplexity is O_of_1];
```
